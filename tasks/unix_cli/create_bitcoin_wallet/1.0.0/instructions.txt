Please create a BitCoin wallet, and save the address and private key on disk to a file at /root/wallet.json
You will probably need to locate a suitable command line tool online to do this, download and install it, and then use it.
Note that you are running as the root user, so will not need to use sudo.